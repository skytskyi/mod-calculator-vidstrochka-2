{
  "name": "mod-calculator-vidstrochka-2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/deferral-form.js --bundle --outfile=deferral-app.js --format=esm",
    "build:browser": "python3 scripts/build_browser.py",
    "test": "vitest run"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.0.5"
  }
}
